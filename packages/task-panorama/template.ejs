<root>
  <scripts><% for (const { path } of htmlWebpackPlugin.files.js) { %>
    <include src="<%= path %>" /><% } %>
  </scripts>
  <Panel><% for (const { type, file } of Object.values(htmlWebpackPlugin.files.xml)) { %>
    <CustomUIElement type="<%= type %>" layoutfile="<%= file %>" /><% } %>
  </Panel>
</root>
