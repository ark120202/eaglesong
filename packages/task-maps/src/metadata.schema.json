{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "A list of playable maps and their extra data.",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "required": ["MaxPlayers"],
    "properties": {
      "MaxPlayers": { "type": "integer", "minimum": 1 }
    }
  }
}
