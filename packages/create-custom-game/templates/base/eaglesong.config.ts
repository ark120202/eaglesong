import { Options } from 'eaglesong';

const config: Options = {
  publish: {
    strategies: {
      release: {
        workshopId: 0,
      },
    },
  },
  tasks: $$tasks$$,
};

export default config;
